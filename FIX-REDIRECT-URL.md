# 🔧 Fix: Redirect URI Not Whitelisted

## ❌ المشكلة:
```
Oauth error invalid_request: The redirect_uri is not whitelisted
```

## ✅ الحل (خطوة بخطوة):

### 1️⃣ افتح Shopify Partners Dashboard
رابط: https://partners.shopify.com

### 2️⃣ اذهب إلى Apps
- من القائمة الجانبية، اضغط **Apps**
- اختار الـ App بتاعك (مثلاً: "WhatsApp CRM")

### 3️⃣ اذهب إلى Configuration
- اضغط على **Configuration** tab
- أو **App setup**

### 4️⃣ أضف Redirect URL
في قسم **App URL and redirect URLs**:

**أ. App URL:**
```
https://nonsaturated-dennis-noncosmically.ngrok-free.dev
```

**ب. Allowed redirection URL(s):**
اضغط **Add URL** وضع:
```
https://nonsaturated-dennis-noncosmically.ngrok-free.dev/api/shopify/oauth/callback
```

⚠️ **تأكد من:**
- ✅ يبدأ بـ `https://` (مش `http://`)
- ✅ نفس الـ ngrok URL بالضبط
- ✅ `/api/shopify/oauth/callback` في الآخر
- ✅ بدون `/` إضافي في النهاية

### 5️⃣ احفظ التغييرات
- اضغط **Save** في أعلى الصفحة
- انتظر رسالة التأكيد

### 6️⃣ تأكد من API Scopes
في نفس الصفحة، تحت **Admin API access scopes**:
- اضغط **Configure**
- تأكد من تفعيل:
  - ✅ `read_orders`
  - ✅ `write_orders`
  - ✅ `read_products`
  - ✅ `read_customers`
  - ✅ `write_fulfillments`
- اضغط **Save**

### 7️⃣ حاول تاني
- ارجع للفرونت إند
- اضغط **Connect with OAuth** تاني
- انسخ الـ URL
- افتحه في تاب جديد
- المفروض يشتغل دلوقتي! ✅

---

## 📸 لقطة شاشة توضيحية:

في Shopify App Configuration، المفروض تشوف:

```
┌─────────────────────────────────────────────────┐
│ App URL                                         │
│ https://nonsaturated-dennis-noncosmically...   │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ Allowed redirection URL(s)                      │
│ https://nonsaturated-dennis-noncosmically...   │
│ /api/shopify/oauth/callback                     │
│                                                 │
│ [+ Add URL]                                     │
└─────────────────────────────────────────────────┘
```

---

## 🔍 استكشاف الأخطاء:

### لو لسه بيطلع نفس الخطأ:
1. تأكد إنك حفظت التغييرات في Shopify
2. جرب refresh للصفحة
3. تأكد إن الـ URL مكتوب صح بالضبط
4. تأكد إن ngrok لسه شغال (شوف terminal)

### لو ngrok URL اتغير:
1. شوف الـ URL الجديد من ngrok terminal
2. حدث الـ URLs في Shopify App
3. حدث `.env` file
4. حدث `ShopifyOAuth.jsx`
5. حاول تاني

---

## ✅ بعد النجاح:

لما الـ OAuth يشتغل، هتشوف:
1. صفحة Install في Shopify
2. اضغط Install
3. Redirect للفرونت إند
4. رسالة: "✅ Shopify connected successfully!"
5. Status badge يتغير لـ "Connected"

---

**جاهز! 🚀**

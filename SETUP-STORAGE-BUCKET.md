# إعداد Supabase Storage للصور والملفات

## الخطوات المطلوبة

### 1. إنشاء Storage Bucket

1. افتح Supabase Dashboard:
   ```
   https://supabase.com/dashboard/project/rmpgofswkpjxionzythf/storage/buckets
   ```

2. اضغط على **"New bucket"**

3. املأ البيانات:
   - **Name**: `whatsapp-media`
   - **Public bucket**: ✅ اختار (عشان الصور تظهر في الشات)
   - **File size limit**: 50 MB (اختياري)
   - **Allowed MIME types**: اتركها فاضية (للسماح بكل الأنواع)

4. اضغط **"Create bucket"**

### 2. تفعيل RLS Policies (اختياري)

لو عايز تحمي الملفات، ممكن تضيف RLS policies:

```sql
-- السماح للجميع بالقراءة (عرض الصور)
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING ( bucket_id = 'whatsapp-media' );

-- السماح للجميع بالرفع (مؤقتاً للتجربة)
CREATE POLICY "Allow Upload"
ON storage.objects FOR INSERT
WITH CHECK ( bucket_id = 'whatsapp-media' );

-- السماح بالحذف (اختياري)
CREATE POLICY "Allow Delete"
ON storage.objects FOR DELETE
USING ( bucket_id = 'whatsapp-media' );
```

### 3. اختبار الرفع

جرب ترفع صورة من الموقع:
1. افتح شات مع أي عميل
2. اضغط على زر 📎 (Paperclip)
3. اختار صورة
4. اضغط Send

### 4. التحقق من الرفع

لو حصل خطأ في الرفع، شوف الـ Console في المتصفح:
- اضغط F12
- شوف تاب Console
- لو في خطأ "bucket not found"، ارجع للخطوة 1

## الميزات المتاحة الآن

✅ **إرسال الصور**: اختار صورة وابعتها
✅ **إرسال الفيديو**: اختار فيديو وابعته
✅ **إرسال المستندات**: PDF, Word, Excel
✅ **تسجيل صوتي**: اضغط على زر 🎤 وسجل صوتك
✅ **حذف الشات**: اضغط على زر 🗑️ في الهيدر
✅ **عرض الميديا**: الصور والفيديوهات تظهر في الشات
✅ **تحميل الملفات**: اضغط على أي ملف لتحميله

## ملاحظات مهمة

- **حجم الملف**: 
  - الصور: حد أقصى 5 MB
  - الفيديو والملفات: حد أقصى 16 MB

- **الأنواع المدعومة**:
  - صور: JPG, PNG, GIF, WebP
  - فيديو: MP4, MOV, AVI
  - صوت: MP3, OGG, WAV
  - مستندات: PDF, DOC, DOCX, XLS, XLSX

- **التسجيل الصوتي**:
  - يحتاج إذن الميكروفون من المتصفح
  - الصوت يتسجل بصيغة OGG
  - اضغط مرة للبدء ومرة للإيقاف

## استكشاف الأخطاء

### خطأ "bucket not found"
- تأكد إنك عملت الـ bucket باسم `whatsapp-media` بالظبط
- تأكد إن الـ bucket public

### الصور مش بتظهر
- تأكد إن الـ bucket public
- شوف الـ Console في المتصفح
- تأكد من الـ RLS policies

### الرفع بطيء
- الملفات الكبيرة بتاخد وقت
- جرب صورة أصغر للاختبار

## الخطوة التالية

بعد ما تعمل الـ bucket، جرب:
1. ابعت صورة لعميل
2. سجل رسالة صوتية
3. احذف شات قديم
4. استقبل صور من العميل (الـ webhook بيحفظها تلقائياً)

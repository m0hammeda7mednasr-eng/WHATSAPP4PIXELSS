# 🔧 إصلاح مشكلة Phone Number ID

## ❌ المشكلة:
```
Object with ID '1428083912314027' does not exist
```

ده معناه إن الـ **Phone Number ID** غلط.

---

## ✅ الحل الصحيح:

### الخطوة 1: روح Meta Developer Console

1. **افتح:** https://developers.facebook.com/apps
2. **اختار الـ App** بتاعك
3. **من القائمة الجانبية:** اضغط **WhatsApp** → **API Setup**

---

### الخطوة 2: لاقي الـ Phone Number ID الصحيح

في صفحة API Setup، هتلاقي:

#### ❌ **غلط - لا تستخدم:**
- **App ID** (رقم قصير نسبياً)
- **Business Account ID**
- أي ID تاني

#### ✅ **صح - استخدم ده:**
- **Phone Number ID** (رقم طويل جداً، حوالي 15 رقم)
- موجود تحت "From" في قسم "Send and receive messages"
- بيكون جنب رقم الموبايل

**مثال:**
```
Phone number: +1 555-025-3483
Phone number ID: 106540529340398  ← ده اللي محتاجه!
```

---

### الخطوة 3: انسخ الـ Token الصحيح

في نفس الصفحة:

1. **لاقي:** "Temporary access token"
2. **اضغط:** Copy
3. **الـ Token بيبدأ بـ:** `EAAxxxxxxxxxxxxxxx`

⚠️ **مهم:** الـ Token ده صالح لـ **24 ساعة** بس!

---

### الخطوة 4: حطهم في Settings

1. **افتح الـ App:** http://localhost:5177
2. **اضغط Settings** (⚙️)
3. **scroll لـ "WhatsApp Brands"**
4. **اضغط Edit**
5. **حط:**
   - **WhatsApp Access Token:** `EAAxxxxxxxxxxxxxxx`
   - **Phone Number ID:** `106540529340398` (الرقم الطويل)
6. **اضغط Save**

---

## 🧪 اختبار:

بعد ما تحط الـ IDs الصحيحة:

1. **جرب تبعت رسالة** من الـ App
2. **شوف الـ server logs** - المفروض تشوف:
   ```
   📤 Sending to WhatsApp API...
   ✅ Message sent to WhatsApp: wamid.xxxxx
   ```

---

## 📸 صورة توضيحية:

في صفحة Meta API Setup، هتلاقي حاجة زي كده:

```
┌─────────────────────────────────────────┐
│ Send and receive messages               │
├─────────────────────────────────────────┤
│ From:                                   │
│ +1 555-025-3483                         │
│ Phone number ID: 106540529340398  ← ده! │
│                                         │
│ To:                                     │
│ [Add recipient phone number]            │
│                                         │
│ Temporary access token:                 │
│ EAAxxxxxxxxxxxxxxx  [Copy]  ← وده!      │
└─────────────────────────────────────────┘
```

---

## ⚠️ لو لسه مش شغال:

### تأكد من:

1. **الـ Token صالح** (مش expired)
2. **الـ Phone Number ID صحيح** (15 رقم تقريباً)
3. **الـ Token عنده permissions** على الـ Phone Number ده
4. **الـ WhatsApp Business Account** مفعّل

### جرب:

```bash
# اختبر الـ API مباشرة
curl -X POST \
  "https://graph.facebook.com/v18.0/YOUR_PHONE_NUMBER_ID/messages" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "messaging_product": "whatsapp",
    "to": "201234567890",
    "type": "text",
    "text": { "body": "Test message" }
  }'
```

لو رجعلك error، يبقى المشكلة في الـ Token أو الـ Phone Number ID.

---

## 💡 نصيحة:

**Permanent Token** (بدل Temporary):

الـ Temporary Token بيخلص كل 24 ساعة. عشان تعمل Permanent Token:

1. **Business Verification** (محتاج documents)
2. **System User** في Business Manager
3. **Generate Permanent Token**

بس ده محتاج وقت ومستندات. للتجربة، استخدم Temporary Token وجدده كل يوم.

---

**محتاج مساعدة؟** ابعتلي screenshot من صفحة API Setup وأنا هساعدك! 📸

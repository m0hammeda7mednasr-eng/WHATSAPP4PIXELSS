# ✅ تفعيل إعدادات الرسائل - Template Settings

## 🎯 الوضع الحالي

تم إنشاء:
- ✅ واجهة Template Settings كاملة
- ✅ SQL لإضافة الأعمدة للداتابيز
- ✅ تكامل مع Settings (تاب جديد)

---

## 📋 الخطوات المطلوبة

### 1️⃣ تشغيل SQL في Supabase

افتح Supabase Dashboard:
```
https://supabase.com/dashboard/project/rmpgofswkpjxionzythf/editor
```

انسخ والصق الكود ده في SQL Editor:

```sql
-- Add Template Settings to brands table

-- Add columns for template configuration
ALTER TABLE brands ADD COLUMN IF NOT EXISTS template_name TEXT DEFAULT 'moon_order_confirmation';
ALTER TABLE brands ADD COLUMN IF NOT EXISTS template_language TEXT DEFAULT 'ar';
ALTER TABLE brands ADD COLUMN IF NOT EXISTS template_header TEXT;
ALTER TABLE brands ADD COLUMN IF NOT EXISTS template_body TEXT;
ALTER TABLE brands ADD COLUMN IF NOT EXISTS template_footer TEXT;
ALTER TABLE brands ADD COLUMN IF NOT EXISTS use_template BOOLEAN DEFAULT true;

-- Add columns for message customization
ALTER TABLE brands ADD COLUMN IF NOT EXISTS brand_emoji TEXT DEFAULT '🌙';
ALTER TABLE brands ADD COLUMN IF NOT EXISTS welcome_message TEXT DEFAULT 'أهلاً بك في {{brand_name}}';
ALTER TABLE brands ADD COLUMN IF NOT EXISTS confirmation_message TEXT DEFAULT 'تم تأكيد طلبك بنجاح!';
ALTER TABLE brands ADD COLUMN IF NOT EXISTS cancellation_message TEXT DEFAULT 'تم إلغاء طلبك.';
```

اضغط **RUN** ✅

---

### 2️⃣ رفع التحديثات على Vercel

```bash
git add .
git commit -m "Add Template Settings UI"
git push origin main
```

Vercel هيعمل Deploy تلقائي 🚀

---

### 3️⃣ تجربة الميزة

1. افتح الموقع: https://wahtsapp2.vercel.app
2. سجل دخول
3. اضغط على **Settings** ⚙️
4. اختر تاب **Message Templates** 💬
5. عدل الإعدادات:
   - اسم الـ Template
   - اللغة
   - رمز البراند (Emoji)
   - رسالة الترحيب
   - رسالة التأكيد
   - رسالة الإلغاء
6. اضغط **حفظ الإعدادات** 💾
7. اضغط **معاينة الرسالة** 👁️ عشان تشوف الشكل النهائي

---

## 🎨 المميزات الجديدة

### كل براند يقدر:
- ✅ يختار اسم Template خاص بيه
- ✅ يغير اللغة (عربي/إنجليزي)
- ✅ يضيف Emoji مميز للبراند
- ✅ يعدل رسالة الترحيب
- ✅ يخصص رسالة التأكيد
- ✅ يخصص رسالة الإلغاء
- ✅ يشوف معاينة الرسالة قبل الحفظ
- ✅ يفعل/يعطل Template Messages

---

## 📱 كيف تشتغل؟

### عند إنشاء طلب جديد:

1. **Shopify** يبعت Webhook للسيستم
2. السيستم يقرأ إعدادات البراند من الداتابيز
3. يبني الرسالة حسب الإعدادات المخصصة
4. يبعت الرسالة للعميل على WhatsApp
5. العميل يضغط **تأكيد** أو **إلغاء**
6. السيستم يبعت الرسالة المخصصة (تأكيد/إلغاء)
7. يحدث حالة الطلب في Shopify

---

## 🔄 الخطوة التالية (اختياري)

لو عايز السيستم يستخدم الإعدادات المخصصة في الرسائل، محتاج تعديل بسيط في:

**api/shopify/webhook-handler.js**

هنضيف قراءة الإعدادات من جدول brands واستخدامها في بناء الرسالة.

---

## ✅ تم!

دلوقتي كل براند عنده:
- ✅ واجهة كاملة لتخصيص الرسائل
- ✅ معاينة مباشرة
- ✅ حفظ في الداتابيز
- ✅ جاهز للاستخدام

---

## 🆘 لو حصلت مشكلة

### المشكلة: التاب مش ظاهر
**الحل**: تأكد إنك عملت git push و Vercel عمل deploy

### المشكلة: خطأ عند الحفظ
**الحل**: تأكد إنك شغلت SQL في Supabase

### المشكلة: الإعدادات مش بتطبق على الرسائل
**الحل**: محتاج تعديل في webhook-handler.js (قول لي وهعمله)

---

**🎉 مبروك! النظام بقى احترافي وكل براند يقدر يخصص رسائله**
